# Verified against Python 3.14.6 on Windows (all wheels available, no source builds).
duckdb==1.5.5
pandas==3.0.5
numpy==2.5.1
scipy==1.18.0
matplotlib==3.11.1

# Notebook walkthrough
jupyter==1.1.1

# Demo console. The UI is plain HTML, CSS and JavaScript served as static files,
# so there is no Node toolchain, no bundler and nothing fetched from a CDN at
# runtime. The container works with no network access.
fastapi==0.140.7
uvicorn[standard]==0.51.0

# Test suite. httpx is what FastAPI's TestClient drives requests through.
pytest==9.1.1
httpx==0.28.1
