[tool.pytest.ini_options]
# Put the project root on sys.path so tests import `src` the same way the
# application does, without needing an editable install.
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
